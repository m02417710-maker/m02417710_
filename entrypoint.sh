#!/bin/bash
# ─────────────────────────────────────────────
#  Entrypoint Script - EGX Scraper
# ─────────────────────────────────────────────

echo "🚀 تشغيل EGX Scraper Container"
echo "📅 $(date '+%Y-%m-%d %H:%M:%S')"
echo "─────────────────────────────────────────────"

# التحقق من وجود قاعدة البيانات
if [ ! -f "$DB_PATH" ]; then
    echo "⚠️  قاعدة البيانات غير موجودة. سيتم إنشاؤها تلقائياً."
fi

# تنفيذ الأمر المُمرر
exec "$@"
